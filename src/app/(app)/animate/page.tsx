import { PlaceholderScreen } from '@/components/shell/PlaceholderScreen';

export const metadata = { title: 'animate · vitrine' };

export default function AnimatePage() {
  return (
    <PlaceholderScreen
      eyebrow="animate"
      title="motion, on demand."
      body="turn a static post into a reel-ready clip. landing soon."
    />
  );
}
